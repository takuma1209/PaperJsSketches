
let skethName = "狂乱"


function drawSpiral(x,y){
    let count = 1000;
    let angle = Math.PI * 0.36;
    let length = 10 + Math.random() * 10;
    let dl = 0.1 + Math.random() * 0.9;
    let randomRange = 0;

    let points =[];
    let theata = Math.PI * 0.25;

    for (let i = 0;i < count; i++){
        points.push([x,y]);
　

     }
}